{
	"private": true,
	"scripts": {
		"build": "nuxt build",
		"dev": "doppler run -- nuxt dev -o --clipboard --debug",
		"generate": "doppler run -- nuxt generate",
		"preview": "doppler run -- nuxt preview",
		"deploy": "doppler run -- nuxt build && firebase deploy --only hosting",
		"start": "node .output/server/index.mjs"
	},
	"devDependencies": {
		"@emailjs/browser": "^3.6.2",
		"@nuxt/ui": "^0.2.0",
		"firebase": "^9.9.2",
		"nuxt": "npm:nuxt3@latest"
	},
	"dependencies": {
		"@octokit/core": "^4.0.5",
		"bulma": "^0.9.4",
		"sass": "^1.54.5"
	}
}
